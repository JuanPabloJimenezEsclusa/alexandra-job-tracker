<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>4.0.6</version>
    <relativePath/>
  </parent>

  <groupId>com.jobtracker</groupId>
  <artifactId>alexandra-job-tracker</artifactId>
  <version>1.0.0</version>
  <packaging>pom</packaging>
  <name>alexandra-job-tracker</name>
  <description>
    Job application tracking system built with Java and Spring Boot, designed to help users manage their job applications efficiently.
    It provides features such as application status tracking, interview scheduling, and analytics to optimize the job search process.
  </description>
  <url>https://juanpablojimenezesclusa.github.io/alexandra-job-tracker</url>

  <organization>
    <name>JPJE Innovation Inc</name>
  </organization>

  <distributionManagement>
    <site>
      <id>github-pages</id>
      <url>file:./target/site-staging</url>
    </site>
  </distributionManagement>

  <licenses>
    <license>
      <name>GPL-3.0-only</name>
      <url>https://www.gnu.org/licenses/gpl-3.0-standalone.html</url>
      <distribution>repo</distribution>
    </license>
  </licenses>

  <developers>
    <developer>
      <id>juan.pablo.jimenez.esclusa</id>
      <name>Juan Pablo Jimenez Esclusa</name>
      <email>juan.pablo.jimenez.esclusa@gmail.com</email>
      <organization>${project.organization.name}</organization>
      <roles>
        <role>Developer</role>
        <role>Architect</role>
      </roles>
    </developer>
  </developers>

  <modules>
    <module>domain</module>
    <module>application</module>
    <module>infrastructure-persistence</module>
    <module>infrastructure-auth</module>
    <module>infrastructure-ai</module>
    <module>infrastructure-observability</module>
    <module>adapter-api</module>
    <module>adapter-cli</module>
    <module>bootstrap-server</module>
    <module>infrastructure-cache</module>
    <module>coverage-jacoco</module>
    <module>bootstrap-cli</module>
  </modules>

  <properties>
    <java.version>25</java.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

    <spring-ai.version>2.0.0-RC2</spring-ai.version>
    <archunit.version>1.4.2</archunit.version>
    <jjwt.version>0.13.0</jjwt.version>
    <spring-shell.version>4.0.2</spring-shell.version>
    <graphql-java-extended-scalars.version>24.0</graphql-java-extended-scalars.version>
    <opentelemetry-exporter-otlp.version>1.63.0</opentelemetry-exporter-otlp.version>
    <caffeine.version>3.2.0</caffeine.version>
    <jspecify.version>1.0.0</jspecify.version>
    <checkstyle.version>13.5.0</checkstyle.version>
    <jackson-jq.version>1.6.2</jackson-jq.version>
    <instancio.version>5.6.0</instancio.version>

    <!-- Cli testing -->
    <wiremock.version>3.13.2</wiremock.version>

    <!-- Site reports -->
    <inceptionYear>2026</inceptionYear>
    <maven.build.timestamp.format>yyyy</maven.build.timestamp.format>
    <currentYear>${maven.build.timestamp}</currentYear>

    <maven-jxr-plugin.version>3.6.0</maven-jxr-plugin.version>
    <maven-surefire-report-plugin.version>3.5.6</maven-surefire-report-plugin.version>
    <reflow-maven-skin.version>2.4.1</reflow-maven-skin.version>

    <!-- plugin versions -->
    <git-commit-id-maven-plugin.version>10.0.0</git-commit-id-maven-plugin.version>
    <git-changelog-maven-plugin.version>2.2.11</git-changelog-maven-plugin.version>
    <jacoco-maven-plugin.version>0.8.15</jacoco-maven-plugin.version>
    <rewrite-maven-plugin.version>6.41.0</rewrite-maven-plugin.version>
    <pitest-maven.version>1.25.4</pitest-maven.version>
    <maven-checkstyle-plugin.version>3.6.0</maven-checkstyle-plugin.version>
    <sonar-maven-plugin.version>5.7.0.6970</sonar-maven-plugin.version>
    <maven-site-plugin.version>3.22.0</maven-site-plugin.version>

    <!-- Sonar -->
    <sonar.organization>juanpablojimenezesclusa</sonar.organization>
    <sonar.host.url>https://sonarcloud.io</sonar.host.url>
    <sonar.projectKey>JuanPabloJimenezEsclusa_alexandra-job-tracker</sonar.projectKey>
    <sonar.coverage.jacoco.xmlReportPaths>
      ${project.basedir}/coverage-jacoco/target/site/jacoco-aggregate/jacoco.xml,
      ${project.basedir}/../coverage-jacoco/target/site/jacoco-aggregate/jacoco.xml
    </sonar.coverage.jacoco.xmlReportPaths>
    <sonar.coverage.exclusions>**/*Config.java,**/*Mapper.java,**/*Hints.java</sonar.coverage.exclusions>
    <sonar.scm.exclusions.disabled>true</sonar.scm.exclusions.disabled>
    <sonar.branch.name>develop</sonar.branch.name>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.springframework.ai</groupId>
        <artifactId>spring-ai-bom</artifactId>
        <version>${spring-ai.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>org.springframework.shell</groupId>
        <artifactId>spring-shell-starter</artifactId>
        <version>${spring-shell.version}</version>
      </dependency>
      <dependency>
        <groupId>org.springframework.shell</groupId>
        <artifactId>spring-shell-core</artifactId>
        <version>${spring-shell.version}</version>
      </dependency>
      <dependency>
        <groupId>org.springframework.shell</groupId>
        <artifactId>spring-shell-jline</artifactId>
        <version>${spring-shell.version}</version>
      </dependency>
      <dependency>
        <groupId>io.jsonwebtoken</groupId>
        <artifactId>jjwt-api</artifactId>
        <version>${jjwt.version}</version>
      </dependency>
      <dependency>
        <groupId>io.jsonwebtoken</groupId>
        <artifactId>jjwt-impl</artifactId>
        <version>${jjwt.version}</version>
      </dependency>
      <dependency>
        <groupId>io.jsonwebtoken</groupId>
        <artifactId>jjwt-jackson</artifactId>
        <version>${jjwt.version}</version>
        <scope>runtime</scope>
      </dependency>
      <dependency>
        <groupId>net.thisptr</groupId>
        <artifactId>jackson-jq</artifactId>
        <version>${jackson-jq.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.assertj</groupId>
      <artifactId>assertj-core</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.jspecify</groupId>
      <artifactId>jspecify</artifactId>
      <version>${jspecify.version}</version>
    </dependency>
    <dependency>
      <groupId>org.instancio</groupId>
      <artifactId>instancio-core</artifactId>
      <version>${instancio.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-checkstyle-plugin</artifactId>
          <version>${maven-checkstyle-plugin.version}</version>
          <configuration>
            <configLocation>${session.executionRootDirectory}/src/main/resources/checkstyle.xml
            </configLocation>
            <consoleOutput>true</consoleOutput>
            <failsOnError>false</failsOnError>
            <includeTestSourceDirectory>true</includeTestSourceDirectory>
          </configuration>
          <dependencies>
            <dependency>
              <groupId>com.puppycrawl.tools</groupId>
              <artifactId>checkstyle</artifactId>
              <version>${checkstyle.version}</version>
            </dependency>
          </dependencies>
          <executions>
            <execution>
              <id>checkstyle-check</id>
              <goals>
                <goal>check</goal>
              </goals>
              <phase>validate</phase>
            </execution>
          </executions>
        </plugin>
        <plugin>
          <groupId>org.openrewrite.maven</groupId>
          <artifactId>rewrite-maven-plugin</artifactId>
          <version>${rewrite-maven-plugin.version}</version>
          <configuration>
            <activeRecipes>
              <recipe>org.openrewrite.maven.BestPractices</recipe>
              <recipe>org.openrewrite.maven.ModernizeObsoletePoms</recipe>
              <recipe>org.openrewrite.maven.OrderPomElements</recipe>
              <recipe>org.openrewrite.yaml.format.Indents</recipe>
              <recipe>org.openrewrite.java.format.EmptyNewlineAtEndOfFile</recipe>
              <recipe>org.openrewrite.java.format.MethodParamPad</recipe>
              <recipe>org.openrewrite.java.format.NormalizeFormat</recipe>
              <recipe>org.openrewrite.java.format.NoWhitespaceAfter</recipe>
              <recipe>org.openrewrite.java.format.NoWhitespaceBefore</recipe>
              <recipe>org.openrewrite.java.format.OperatorWrap</recipe>
              <recipe>org.openrewrite.java.format.PadEmptyForLoopComponents</recipe>
              <recipe>org.openrewrite.java.format.RemoveTrailingWhitespace</recipe>
              <recipe>org.openrewrite.java.format.Spaces</recipe>
              <recipe>org.openrewrite.java.OrderImports</recipe>
              <recipe>org.openrewrite.java.RemoveUnusedImports</recipe>
            </activeRecipes>
            <activeStyles>
              <style>org.openrewrite.java.IntelliJ</style>
              <style>org.openrewrite.java.SpringFormat</style>
            </activeStyles>
            <failOnDryRunResults>false</failOnDryRunResults>
            <sizeThresholdMb>20</sizeThresholdMb>
            <plainTextMasks>
              <plainTextMask>**/.txt</plainTextMask>
            </plainTextMasks>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.sonarsource.scanner.maven</groupId>
          <artifactId>sonar-maven-plugin</artifactId>
          <version>${sonar-maven-plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.jacoco</groupId>
          <artifactId>jacoco-maven-plugin</artifactId>
          <version>${jacoco-maven-plugin.version}</version>
          <configuration>
            <excludes>
              <exclude>**/*Config.class</exclude>
              <exclude>**/*Mapper.class</exclude>
              <exclude>**/*Hints.class</exclude>
            </excludes>
          </configuration>
          <executions>
            <execution>
              <id>default-prepare-agent</id>
              <goals>
                <goal>prepare-agent</goal>
              </goals>
            </execution>
            <execution>
              <id>default-prepare-agent-integration</id>
              <goals>
                <goal>prepare-agent-integration</goal>
              </goals>
            </execution>
            <execution>
              <id>merge-results</id>
              <goals>
                <goal>merge</goal>
              </goals>
              <phase>verify</phase>
              <configuration>
                <fileSets>
                  <fileSet>
                    <directory>${project.build.directory}</directory>
                    <includes>
                      <include>*.exec</include>
                    </includes>
                  </fileSet>
                </fileSets>
                <destFile>${project.build.directory}/jacoco-aggregate.exec</destFile>
              </configuration>
            </execution>
            <execution>
              <id>report</id>
              <goals>
                <goal>report</goal>
              </goals>
              <phase>verify</phase>
              <configuration>
                <dataFile>${project.build.directory}/jacoco-aggregate.exec</dataFile>
                <excludes>
                  <exclude>**/*Config.class</exclude>
                  <exclude>**/*Mapper.class</exclude>
                  <exclude>**/*Hints.class</exclude>
                </excludes>
              </configuration>
            </execution>
          </executions>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-enforcer-plugin</artifactId>
          <executions>
            <execution>
              <id>enforce</id>
              <goals>
                <goal>enforce</goal>
              </goals>
              <configuration>
                <rules>
                  <banDuplicatePomDependencyVersions/>
                  <requireReleaseDeps/>
                  <requireUpperBoundDeps>
                    <excludes>
                      <exclude>org.springframework.boot:spring-boot-starter</exclude>
                      <exclude>org.springframework.boot:spring-boot-starter-restclient</exclude>
                      <exclude>org.springframework.boot:spring-boot-starter-webclient</exclude>
                      <exclude>com.fasterxml.jackson.module:jackson-module-kotlin</exclude>
                      <exclude>io.micrometer:micrometer-core</exclude>
                      <exclude>io.micrometer:micrometer-observation</exclude>
                      <exclude>org.jetbrains.kotlin:kotlin-reflect</exclude>
                      <exclude>io.opentelemetry:opentelemetry-sdk-metrics</exclude>
                      <exclude>io.opentelemetry:opentelemetry-exporter-otlp</exclude>
                      <exclude>io.opentelemetry:opentelemetry-exporter-otlp-common</exclude>
                      <exclude>io.opentelemetry:opentelemetry-sdk-logs</exclude>
                      <exclude>io.opentelemetry:opentelemetry-exporter-sender-okhttp</exclude>
                      <exclude>io.opentelemetry:opentelemetry-sdk-trace</exclude>
                      <exclude>io.opentelemetry:opentelemetry-sdk-extension-autoconfigure-spi</exclude>
                    </excludes>
                  </requireUpperBoundDeps>
                  <requireMavenVersion>
                    <version>[3.9.4,4.0.0)</version>
                  </requireMavenVersion>
                  <requireJavaVersion>
                    <version>[25,30)</version>
                  </requireJavaVersion>
                </rules>
              </configuration>
            </execution>
          </executions>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-site-plugin</artifactId>
          <version>${maven-site-plugin.version}</version>
          <configuration>
            <generateSitemap>true</generateSitemap>
            <stagingDirectory>${project.build.directory}/site-staging</stagingDirectory>
            <attributes>
              <reporting.outputEncoding>${project.reporting.outputEncoding}
              </reporting.outputEncoding>
              <project.build.sourceEncoding>${project.build.sourceEncoding}
              </project.build.sourceEncoding>
              <currentYear>${currentYear}</currentYear>
              <inceptionYear>${inceptionYear}</inceptionYear>
            </attributes>
          </configuration>
          <dependencies>
            <dependency>
              <groupId>io.github.devacfr.maven.skins</groupId>
              <artifactId>reflow-maven-skin</artifactId>
              <version>${reflow-maven-skin.version}</version>
            </dependency>
            <dependency>
              <groupId>io.github.devacfr.maven.skins</groupId>
              <artifactId>reflow-velocity-tools</artifactId>
              <version>${reflow-maven-skin.version}</version>
            </dependency>
          </dependencies>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-jxr-plugin</artifactId>
          <version>${maven-jxr-plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-report-plugin</artifactId>
          <version>${maven-surefire-report-plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-javadoc-plugin</artifactId>
          <version>${maven-javadoc-plugin.version}</version>
          <configuration>
            <source>${java.version}</source>
            <encoding>${project.reporting.outputEncoding}</encoding>
            <additionalJOptions>-Xdoclint:none</additionalJOptions>
            <useStandardDocletOptions>true</useStandardDocletOptions>
            <failOnError>false</failOnError>
            <show>package</show>
            <doclint>none</doclint>
            <windowtitle>${project.name} ${project.version} API</windowtitle>
            <doctitle>
              <![CDATA[<h1>${project.name} API Documentation</h1><h3>${project.description}</h3>]]></doctitle>
            <header><![CDATA[<b>${project.name}</b> v${project.version}]]></header>
            <bottom>
              <![CDATA[Copyright &#169; ${inceptionYear}–${currentYear} | ${project.organization.name}. | All rights reserved.]]></bottom>
          </configuration>
          <executions>
            <execution>
              <id>aggregate-javadocs</id>
              <goals>
                <goal>aggregate</goal>
              </goals>
            </execution>
          </executions>
        </plugin>
        <plugin>
          <groupId>se.bjurr.gitchangelog</groupId>
          <artifactId>git-changelog-maven-plugin</artifactId>
          <version>${git-changelog-maven-plugin.version}</version>
          <inherited>false</inherited>
          <executions>
            <execution>
              <id>GenerateGitChangelog</id>
              <phase>generate-sources</phase>
              <goals>
                <goal>git-changelog</goal>
              </goals>
              <configuration>
                <templateFile>${project.basedir}/src/main/resources/changelog.mustache
                </templateFile>
                <file>CHANGELOG.md</file>
                <readableTagName>-([^-]+?)$</readableTagName>
                <dateFormat>yyyy-MM-dd HH:mm:ss</dateFormat>
                <ignoreCommitsIfMessageMatches>^\[maven-release-plugin\].*
                </ignoreCommitsIfMessageMatches>
                <timeZone>UTC</timeZone>
                <removeIssueFromMessage>true</removeIssueFromMessage>
              </configuration>
            </execution>
          </executions>
        </plugin>
      </plugins>
    </pluginManagement>

    <plugins>
      <plugin>
        <groupId>org.openrewrite.maven</groupId>
        <artifactId>rewrite-maven-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-checkstyle-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-enforcer-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-javadoc-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>se.bjurr.gitchangelog</groupId>
        <artifactId>git-changelog-maven-plugin</artifactId>
      </plugin>
    </plugins>

    <extensions>
      <extension>
        <groupId>kr.motd.maven</groupId>
        <artifactId>os-maven-plugin</artifactId>
        <version>1.7.1</version>
      </extension>
    </extensions>
  </build>

  <reporting>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jxr-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-report-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <reportSets>
          <reportSet>
            <reports>
              <report>report</report>
            </reports>
          </reportSet>
        </reportSets>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-checkstyle-plugin</artifactId>
        <reportSets>
          <reportSet>
            <id>checkstyle-report</id>
            <reports>
              <report>checkstyle</report>
            </reports>
          </reportSet>
        </reportSets>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-javadoc-plugin</artifactId>
        <reportSets>
          <reportSet>
            <id>aggregate-javadocs</id>
            <reports>
              <report>aggregate</report>
            </reports>
          </reportSet>
        </reportSets>
      </plugin>
    </plugins>
  </reporting>

  <profiles>
    <profile>
      <id>pitest</id>
      <build>
        <pluginManagement>
          <plugins>
            <plugin>
              <groupId>org.pitest</groupId>
              <artifactId>pitest-maven</artifactId>
              <version>${pitest-maven.version}</version>
              <dependencies>
                <dependency>
                  <groupId>org.pitest</groupId>
                  <artifactId>pitest-junit5-plugin</artifactId>
                  <version>1.2.3</version>
                </dependency>
              </dependencies>
              <configuration>
                <verbose>true</verbose>
                <threads>6</threads>
                <timeoutFactor>1.50</timeoutFactor>
                <timeoutConstant>5000</timeoutConstant>
                <failWhenNoMutations>false</failWhenNoMutations>
                <timestampedReports>false</timestampedReports>
                <exportLineCoverage>true</exportLineCoverage>
                <outputFormats>
                  <value>XML</value>
                </outputFormats>
                <excludedClasses>
                  <param>**.config.*</param>
                  <param>**.*Config</param>
                </excludedClasses>
                <excludedTestClasses>
                  <param>**.architecture.*</param>
                </excludedTestClasses>
              </configuration>
            </plugin>
          </plugins>
        </pluginManagement>
        <plugins>
          <plugin>
            <groupId>org.pitest</groupId>
            <artifactId>pitest-maven</artifactId>
            <executions>
              <execution>
                <id>pitest</id>
                <goals>
                  <goal>mutationCoverage</goal>
                </goals>
                <phase>test-compile</phase>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
