info:
  name: query.analytics
  type: graphql
  seq: 11

graphql:
  method: POST
  url: "{{baseUrl}}/graphql"
  headers:
    - name: Accept
      value: application/json
  body:
    query: |
      query($since: Instant) {
        analytics(since: $since) {
          totalApplications, perStatus { saved, applied, interviewing, offer, accepted, rejected, withdrawn }, conversionRate
        }
      }
    variables: |-
      {

      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
